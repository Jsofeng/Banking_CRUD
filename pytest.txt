IN ORDER TO RUN PYTEST RUN "DOCKER COMPOSE UP" GO ON TERMINAL backend/ COMMENT Base.metadata.create_all(bind=engine) in main.py then run python -m pytest

"""
pytest
   │
   ▼
AsyncClient
   │
   ▼
FastAPI app (directly in memory)

Test starts
      │
create tables
      │
open database session
      │
run test
      │
close session
      │
delete tables

"""


RUN inside backend/

python -m pytest 


TESTING

- pytest: The test runner — finds and runs all your tests
- pytest-asyncio  Lets pytest handle async FastAPI routes
- httpx HTTP client that simulates real requests to your API


backend/
└── tests/
    ├── __init__.py        ← makes it a Python package (can be empty)
    ├── conftest.py        ← shared setup that all tests can use
    ├── test_auth.py       ← tests for register/login
    └── test_accounts.py   ← tests for your CRUD endpoints


pip install pytest-cov (This runs all your tests and prints a table showing which lines of code were hit and which were missed.)

RUN pytest --cov=. --cov-report=term-missing

Name              Stmts   Miss  Cover
--------------------------------------
main.py              45      3    93%
auth.py              30      0   100%
models.py            12      0   100%

Stmts - total lines of code
Miss - lines no test touched
Cover - percentage covered

RUN pytest --cov=. --cov-report=html
This creates a htmlcov/ folder. Open htmlcov/index.html in your browser to see a visual, line-by-line breakdown of what's covered and what isn't. Green = tested, red = not tested.

pytest -v run every individual test and shows whether passed or not 
python -m pytest test/filename (tests all functions in that file)

pytest tests/test_auth.py::function_name -v (tests that individual function in that file)