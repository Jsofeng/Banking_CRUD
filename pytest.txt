IN ORDER TO RUN PYTEST GO ON TERMINAL backend/ COMMENT Base.metadata.create_all(bind=engine) in main.py then run python -m pytest

"""
pytest
   │
   ▼
AsyncClient
   │
   ▼
FastAPI app (directly in memory)

Test starts
      │
create tables
      │
open database session
      │
run test
      │
close session
      │
delete tables

"""


RUN inside backend/

python -m pytest 