DATABASE

Account
- id
- owner_name
- account_type  (chequing / savings)
- balance
- created_at

BACKEND
AUTHENTICATION & AUTHORIZATION

- python-jose Creates verifies JWT tokens
- passlib Hashes and verifies passwords
- python-multipart Lets FastAPI read form data (needed for login)


FRONTEND 
- npm create vite@latest frontend -- --template react 
- User types → React stores values → Form submits → Backend receives data
- AccountForm - the form to create an account_type (sends POST req to backend)
- AccountCard - displays one account_type (sends GET /account/{id} req to backend)
- AccountList - holds all the cards (sends GET /account req to backend)

* AccountList = manager (owns all accounts)
* AccountCard = worker (shows one account)
* onEdit = “report back to manager”

step 1:
- register

step2
- login
- get access_token

step3
- top right click authorize
- enter user & password 


handleUpdate workflow:

User clicks Save
        |
        v
AccountCard handleUpdate()
        |
        v
PUT /accounts/{id}
        |
        v
FastAPI returns updated account
        |
        v
onEdit(updatedAccount)
        |
        v
AccountList handleEdit()
        |
        v
Replace old account in accounts[]
        |
        v
React re-renders AccountCard


{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJzdHJpbmciLCJleHAiOjE3ODI4NzY1NzV9.qEKDPakbPAnZTXGnVRcjLKilSKmzlUgYBkJiuwjcexw",
  "token_type": "bearer"
}

TESTING

- pytest: The test runner — finds and runs all your tests
- pytest-asyncio  Lets pytest handle async FastAPI routes
- httpx HTTP client that simulates real requests to your API


backend/
└── tests/
    ├── __init__.py        ← makes it a Python package (can be empty)
    ├── conftest.py        ← shared setup that all tests can use
    ├── test_auth.py       ← tests for register/login
    └── test_accounts.py   ← tests for your CRUD endpoints